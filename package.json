{
  "name": "paint",
  "version": "1.0.0",
  "description": "vanillaJS made by paint tool",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel index.html --open",
    "predeploy": "cross-env rm -rf dist && parcel build index.html --public-url /paint-vanillaJS",
    "deploy": "npm run predeploy && gh-pages -d dist"
  },
  "author": "lizzy Hokmah",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel": "^2.0.0-beta.2",
    "webpack-dev-server": "^3.11.2"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "node-sass": "^5.0.0",
    "semver": "^5.7.1"
  }
}
