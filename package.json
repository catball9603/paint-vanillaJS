{
  "name": "paint",
  "version": "1.0.0",
  "description": "vanillaJS made by paint tool",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url YOUR_REPO_NAME"
  },
  "author": "lizzy Hokmah",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4",
    "parcel-bundler": "^1.12.4",
    "webpack-dev-server": "^3.11.2"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
